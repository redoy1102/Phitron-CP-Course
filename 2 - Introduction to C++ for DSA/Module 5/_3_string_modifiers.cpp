#include<bits/stdc++.h>
using namespace std;
void solve(){
    string s, a;
    // cin>>s>>a;
    cin >> s;

    // s += a;
    // s.push_back('F');
    // s.erase(3, 1);
    // s.erase(3, 2);
    // s.insert(0, "Code");
    // cout << *s.begin();
    // cout << *(s.begin() + 1) << endl;
    // cout << *(s.end() - 1) << endl;

    // cout << s << endl;
}
int main(){
    int t;
    // cin >> t;
    t = 1;
    while(t--){
        solve();
    }

    return 0;
}